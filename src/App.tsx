export default function () {
  return (
    <div className="prose container mx-auto flex min-h-screen max-w-prose flex-col items-center justify-center p-10 text-center">
      <h1>$SIMP</h1>
      <h2>Stupid Internet Money Protocol</h2>
      <h3>
        The larger the stake, <b>the more it rewards</b>
      </h3>
      <p>
        ca: <code className="break-all">0x0</code>
      </p>
      <div className="flex flex-row gap-2 flex-wrap items-center justify-center">
        <button className="btn btn-primary">Dex</button>
        <button className="btn btn-primary">Farcaster</button>
        <button className="btn btn-primary">Exp</button>
      </div>
      <p>
        Only 69,420,000 $SIMP will ever exist. 3% to the team. 17% to the
        airdrops. 10% contingency fund locked behind 2/5 multisig between
        <code>borodutch.eth</code>, <code>vitalik.eth</code>,{' '}
        <code>jessepollak.eth</code>, <code>dwr.eth</code>, and{' '}
        <code>varunsrin.eth</code>. The rest goes into the liquidity pool.
      </p>
      <p>
        Airdrop will be proportional to: $SIMP holding, number of Farcaster and
        X posts with "$SIMP" in them. Posts containing "What's $SIMP?" get 2x
        the weight. $SIMP trading volume. And a few secret variables.
      </p>
    </div>
  )
}
